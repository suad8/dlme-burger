// بديل فارغ لحزمة `server-only` أثناء الاختبارات.
//
// الحزمة الأصلية ترمي عند استيرادها خارج Server Component — وهو سلوك مقصود
// في البناء، لكنه يمنع اختبار وحدات الخادم في Node. الحارس يبقى فعّالًا في
// كود المنتج؛ هذا الاستبدال لا يُطبَّق إلا داخل Vitest.
export {}
