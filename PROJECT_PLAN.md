# إتقان — منصة التشغيل للمطاعم والمقاهي

> اسم مؤقت للمشروع. «إتقان» اختير لأن جوهر المنتج هو الالتزام التشغيلي:
> الفحوصات، الزيارات، الإجراءات التصحيحية، وقياس الأداء عبر الفروع.

منصة SaaS سعودية متعددة المستأجرين لإدارة وتشغيل المطاعم والمقاهي والفود ترك
والمطابخ السحابية والعلامات متعددة الفروع.

---

## 1. فحص المستودع قبل التنفيذ

تم فحص المستودع بالكامل قبل أي تعديل. النتيجة:

| العنصر | الحالة قبل التنفيذ |
|---|---|
| بنية الملفات | 5 ملفات فقط: `index.html`, `src/App.jsx`, `src/main.jsx`, `src/index.css`, `tailwind.config.js` |
| `package.json` | تطبيق Vite + React 18 باسم `nmat-menu` — منيو مقهى بصفحة واحدة |
| قاعدة البيانات | **لا توجد** |
| نظام المصادقة | **لا يوجد** |
| ملفات البيئة | **لا توجد** |
| إعدادات البناء | Vite 4، بناء ثابت (static) |
| النشر | GitHub Actions → GitHub Pages (استضافة ثابتة فقط) |
| حالة Git | فرع `main` نظيف، آخر التزام `0a6f41a` |
| تعديلات سابقة | حالة السلة، فلترة التصنيفات، ربط واتساب — **محفوظة بالكامل** |

### قرار معماري رقم 1 — لماذا لم نبنِ فوق Vite

القاعدة 2 تقول: استخدم التقنية الموجودة إن كانت **صالحة**. التقنية الحالية غير
صالحة لهذا المنتج لسبب واحد حاسم:

> المطلوب عزل مستأجرين مفروض **على الخادم وقاعدة البيانات**، وتفويض من جهة
> الخادم (Server-side authorization). تطبيق Vite هو SPA ثابت **بلا خادم
> إطلاقًا** — لا يمكن فرض أي صلاحية فيه، وكل ما يُرسل للمتصفح مكشوف.

لذلك: انتقلنا إلى Next.js App Router (خادم حقيقي + Server Actions + Route
Handlers). التطبيق القديم **لم يُحذف**؛ نُقل عبر `git mv` إلى
`legacy/menu-demo/` مع بقاء تاريخه، ولا يزال يُبنى ويُنشر على GitHub Pages كما
كان.

### قرار معماري رقم 2 — النشر

⚠️ **GitHub Pages لا يصلح للمنصة.** Pages يخدم ملفات ثابتة فقط؛ المنصة تحتاج
Node runtime + PostgreSQL. تفاصيل البدائل في `DEPLOYMENT.md`.

---

## 2. المرجعيات — ما سيُقتبس من كل موقع

اقتباس **مبادئ وأنماط وتجربة استخدام فقط**. لا نسخ هوية أو نصوص أو صور أو CSS
أو كود. هوية «إتقان» البصرية واللغوية أصلية بالكامل.

| المرجع | ما يُقتبس وظيفيًا | ما يُقتبس بصريًا / تجريبيًا | ما لا يُقتبس |
|---|---|---|---|
| **Al Menu** | تصنيف الخدمات التشغيلية ككتالوج قابل للطلب (مدير تشغيل، هندسة منيو، عميل خفي، تدريب)؛ ربط الخدمة بفرع محدد؛ مراحل تنفيذ الطلب | نبرة المحتوى العربي السعودي؛ بناء الثقة بالتدرّج لا بالمبالغة؛ عرض الباقات بلغة تشغيلية لا تقنية | الهوية، الشعار، النصوص، الأسعار، صور العملاء |
| **Toast** | محدّد ثلاثي المستوى (منشأة ← علامة ← فرع)؛ توريث الإعدادات من العلامة للفرع مع إمكانية التخصيص؛ تقارير مركزية تُفلتر بالفرع؛ مقارنة أداء الفروع | سرعة التنقل بين الفروع دون إعادة تحميل السياق؛ وضوح «أين أنا الآن» في الهيدر | التصميم، الألوان، تسمية المنتجات |
| **Restaurant365** | تكلفة الوصفة من المكوّنات؛ نسبة Food Cost؛ الهدر بأسبابه؛ حركة المخزون كسجل غير قابل للتعديل الصامت؛ التنبيه عند تغيّر تكلفة مكوّن | لوحات مؤشرات تربط التشغيل بالمال في شاشة واحدة؛ عرض الاتجاه بجانب الرقم لا منفصلًا | التصميم، هيكل الحسابات، تكاملاتهم |
| **SafetyCulture** | قوالب فحص بأقسام وأنواع أسئلة متعددة؛ رفع صور كدليل؛ التوقيع؛ تحويل المخالفة إلى إجراء تصحيحي بمسؤول وموعد؛ احتساب نتيجة الالتزام | تجربة الجوال أثناء الزيارة الميدانية أولًا لا كتصغير لسطح المكتب؛ منع الإغلاق قبل الحقول الإلزامية | التصميم، صياغة القوالب الجاهزة، الأيقونات |
| **Linear** | Command Menu للتنقل والبحث؛ حالات المهام كسير عمل صريح؛ الفلاتر محفوظة في الـURL؛ نوافذ جانبية بدل تنقل كامل | كثافة معلومات عالية مع هدوء بصري؛ حركة قصيرة (150–250ms)؛ اختصارات لوحة مفاتيح | التصميم، الألوان، الخط، الحركات الحرفية |

---

## 3. الحزمة التقنية المعتمدة

الإصدارات مثبّتة بعد التحقق من npm وقت التنفيذ (2026-08).

| الطبقة | الاختيار | الإصدار | سبب الاختيار |
|---|---|---|---|
| الإطار | Next.js App Router | 16.3.3 | خادم حقيقي، Server Actions، RSC |
| الواجهة | React | 19.2.8 | مرافق لـNext 16 |
| اللغة | TypeScript **strict** | 5.9.3 | 7.x صدر حديثًا ولم يستقر مع توليد أنواع Next بعد |
| التنسيق | Tailwind CSS (CSS-first) | 4.3.3 | `@theme` بدل ملف إعداد JS |
| المكوّنات | Radix UI + مكوّنات بنمط shadcn مكتوبة يدويًا | — | تحكم كامل في RTL والوصول |
| قاعدة البيانات | PostgreSQL | 16.13 | تعمل فعليًا في بيئة التطوير |
| ORM | Prisma | 7.10.0 | 8.0.0 لا يزال RC |
| المصادقة | **Better Auth** | 1.7.1 | إصدار مستقر — بديله Auth.js v5 لا يزال beta |
| التحقق | Zod | 4.4.3 | نفس المخطط على الخادم والعميل |
| النماذج | React Hook Form | 7.86.0 | + `@hookform/resolvers` |
| الرسوم | Recharts | 3.10.1 | — |
| الأيقونات | Lucide | 1.34.0 | مجموعة موحدة |
| اختبار الوحدات | Vitest | 4.1.11 | — |
| اختبار E2E | Playwright | 1.62.1 | — |

**قاعدة صارمة:** ORM واحد (Prisma) ونظام مصادقة واحد (Better Auth). لا خلط.

---

## 4. نموذج المنتج — تعدد المستأجرين

```
Organization (المنشأة)  ← حدّ العزل
  ├── Brand (العلامة التجارية)  1..n
  │     └── Branch (الفرع)  1..n
  ├── Membership (عضوية مستخدم + دور + نطاق فروع)
  ├── Subscription → Plan → PlanFeature
  └── كل بيانات التشغيل (تحمل organizationId إلزاميًا)
```

**فرض العزل على ثلاث طبقات:**

1. **قاعدة البيانات** — `organizationId` في كل جدول مستأجر، مع فهارس مركّبة
   `(organizationId, …)` وقيود مفاتيح أجنبية مركّبة تمنع الربط عبر المنشآت.
2. **الخادم** — كل استعلام يمر عبر `TenantContext` مشتق من الجلسة، ولا يُبنى
   من أي مُدخل من المتصفح. لا يوجد مسار يقبل `organizationId` من العميل.
3. **الاختبارات** — اختبارات تثبت أن مستخدم منشأة (أ) لا يقرأ بيانات (ب)،
   وتفشل البناء إن انكسر العزل.

---

## 5. نطاق MVP مقابل المؤجل

هذا مشروع بحجم فريق لعدة أشهر. النطاق مرتّب بصدق:

### منفّذ في هذه الجولة (Phase 0 → 3)

- الوثائق الأربع + `DEPLOYMENT.md`
- مخطط قاعدة بيانات كامل (43 نموذجًا) + ترحيل مطبّق على PostgreSQL حقيقي
- Better Auth + تسجيل/دخول + جلسات
- Multi-tenancy مفروض على الخادم + RBAC بعشرة أدوار وصلاحيات دقيقة
- Design System + RTL + i18n (ar/en) + صفحة `/design-system`
- Seed عربي واقعي
- الموقع التسويقي العام
- Onboarding متدرّج مع حفظ التقدّم
- لوحة التحكم ببيانات حقيقية
- الفروع، قوالب الفحص، الزيارات، الإجراءات التصحيحية
- اختبارات وحدات + اختبارات عزل مستأجرين

### مؤجّل بوعي (Phase 4 → 6)

| المؤجّل | السبب |
|---|---|
| تكامل بوابة دفع فعلية | يتطلب مفاتيح إنتاج وحساب تاجر — بُنيت طبقة `BillingProvider` مجرّدة جاهزة للربط |
| تقارير PDF | تتطلب خدمة تصيير؛ التصدير إلى CSV منفّذ |
| رسائل SMS / WhatsApp | تتطلب مزوّدًا معتمدًا — بُنيت واجهة القناة |
| عمق التوظيف والمخزون | المخطط والجداول جاهزة؛ الواجهات في المرحلة التالية |
| Playwright E2E موسّع | التهيئة جاهزة مع مسارات أساسية |

**التقرير النهائي في `IMPLEMENTATION_STATUS.md` يوضّح ما اكتمل فعليًا بلا تجميل.**

---

## 6. المخاطر

| الخطر | الأثر | المعالجة |
|---|---|---|
| تسرّب بيانات بين المنشآت | حرج | عزل ثلاثي الطبقات + اختبارات إلزامية |
| GitHub Pages لا يستضيف المنصة | حرج للإطلاق | موثّق في `DEPLOYMENT.md` مع بدائل |
| Better Auth حديث نسبيًا | متوسط | الاعتماد على جلسات قاعدة البيانات؛ منطق RBAC مملوك لنا لا للمكتبة |
| تعارض RTL مع مكتبات الرسوم | متوسط | خصائص منطقية (logical properties) + اختبار الاتجاهين |
| نمو المخطط وN+1 | متوسط | فهارس مركّبة + `select` صريح + ترقيم على الخادم |
| أرقام نجاح غير حقيقية | سمعة | ممنوع؛ كل رقم تجريبي موسوم «بيانات تجريبية» |
