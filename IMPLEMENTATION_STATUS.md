# حالة التنفيذ — إتقان

آخر تحديث: ٢٠٢٦-٠٨-٢٦ · الفرع: `claude/itqan-platform`

> هذه الوثيقة صريحة عمدًا. المطلوب في الطلب الأصلي يعادل عمل فريق لعدة أشهر.
> ما يلي هو ما اكتمل **فعليًا وتم التحقق منه بالتشغيل**، وما لم يُنجز بعد.

---

## نتائج التحقق

كلها شُغّلت فعليًا على هذا الفرع، لا مُدّعاة:

| الفحص | الأمر | النتيجة |
|---|---|---|
| ESLint | `npm run lint` | ✅ صفر أخطاء |
| TypeScript (strict) | `npm run typecheck` | ✅ صفر أخطاء |
| الاختبارات | `npm run test` | ✅ **٩٢ اختبارًا ناجحًا** (٥ ملفات) |
| بناء الإنتاج | `npm run build` | ✅ **٣١ مسارًا** |
| المسارات العامة | فحص HTTP | ✅ ١٤ مسارًا ترجع 200 |
| حماية المسارات | فحص HTTP | ✅ `/dashboard`, `/branches`, `/onboarding` → 307 إلى `/login` |
| التشغيل الفعلي | Playwright | ✅ دخول → كل الوحدات، تنفيذ فحص كامل، صفر أخطاء JS |
| فرض الصلاحيات | فحص حي | ✅ مالك → `/admin` = 404 · مدير نظام = 200 · مُطّلع → تصدير = 403 |
| تصدير CSV | فحص حي | ✅ عربية سليمة بـBOM وترويسات صحيحة |
| بنّاء القوالب | فحص حي | ✅ قالب أُنشئ من الواجهة وحُفظ فعلًا بأقسامه وبنوده وسجل تدقيقه |
| الإشعارات | فحص حي | ✅ الجرس يعرض ٣ إشعارات من قاعدة البيانات |
| Command Menu | فحص حي | ✅ ⌘K يفتح ويبحث بالعربية وينقل فعليًا |

قاعدة البيانات: PostgreSQL 16 حقيقية، **٥٢ جدولًا**، ترحيلان مطبّقان، بيانات
مزروعة (٢ منشأة · ٤ فروع · ٩ مستخدمين · ٥٢ زيارة · ٥٧٦ إجابة · ١٨ إجراءً
تصحيحيًا · ١٢ موظفًا · ٨ وصفات · ٣٦ صنف مخزون).

---

## ✅ مكتمل ومتحقَّق منه

### الأساس

- **مخطط قاعدة بيانات كامل**: ٤٣ نموذجًا يغطي **كل** الجداول المطلوبة في الطلب،
  بمفاتيح أجنبية وفهارس مركّبة و`organizationId` في كل جدول مستأجر و`deletedAt`
  و`version` للتزامن المتفائل.
- **الترحيلات مطبّقة** على PostgreSQL حقيقية — لا `db push` ولا مخطط نظري.
- **المصادقة**: Better Auth 1.7 بجلسات في قاعدة البيانات، كوكيز `httpOnly`،
  كلمة مرور ≥ ١٢ محرفًا.
- **تعدد المستأجرين**: `TenantContext` يُشتق من الجلسة حصريًا. لا مسار يقبل
  `organizationId` من المتصفح.
- **RBAC**: ١٠ أدوار × ١١٢ صلاحية، تُنسخ لكل منشأة فتصبح قابلة للتخصيص.
  `authorize()` قبل كل عملية.
- **نطاق الفروع**: مدير الفرع مقيّد فعليًا — متحقَّق منه في الاختبارات وفي
  المتصفح (شارة «نطاقك: ١ فرع»).

### الأمان

- **٩ اختبارات عزل** تفشل البناء عند التسرّب، منها ٤ استعلامات SQL تتحقق من
  سلامة المرجعية عبر الجداول.
- **قاعدة ESLint** ترفض استيراد Prisma خارج طبقة الخادم — **أمسكت ٤ مخالفات
  فعلية أثناء التطوير** فأُعيدت توجيهها عبر طبقة الخدمات.
- تنقية سجل التدقيق (`redact`) مع اختبارات تشمل المفاتيح المتداخلة والمصفوفات
  واختلاف الشكل الكتابي.
- تحديد معدّل على الدخول والتسجيل.
- حارس إقلاع يمنع الإنتاج بمفتاح تطويري — **فعّل نفسه فعلًا وأوقف أول بناء**.
- منع IDOR: 404 بدل 403 حتى لا يُكشف وجود السجل.
- حدود الباقة مفروضة على الخادم في `createBranch`.

### الواجهة

- **نظام تصميم أصلي**: tokens بـTailwind v4 CSS-first، وضع داكن كامل،
  `prefers-reduced-motion` محترم، أهداف نقر ≥ 44px، حالات focus موحّدة.
- **RTL حقيقي**: خصائص منطقية (`ms-*`, `me-*`, `start-*`) لا `ml-*`.
- **الموقع العام**: ١٣ صفحة — رئيسية بواجهة منتج حقيقية في الـHero، مميزات،
  حلول، خدمات، أسعار (من قاعدة البيانات)، أسئلة شائعة بـSchema.org، من نحن،
  قصص نجاح، تواصل، خصوصية، شروط، دخول، تسجيل.
- **لوحة تحكم ببيانات حقيقية**: درجة الالتزام، مقارنة الفروع، اتجاه زمني،
  نشاطات أخيرة — كلها استعلامات مجمّعة (`groupBy` / SQL) لا N+1.
- **صفحة `/design-system`** تعرض الألوان والخطوط والأزرار والحقول والجداول
  والرسوم وحالات التحميل والفراغ والخطأ.
- **نظام حركة**: منحنيات مخصّصة، ظهور عند التمرير، توهّج يتتبّع المؤشر، عدّاد
  تصاعدي، شريط متحرّك — كله يُطفأ بالكامل عند `prefers-reduced-motion`.
- **وضع داكن قابل للتبديل** بلا وميض، عبر سكربت يسبق أول رسم.
- **تجربة جوال مخصّصة**: تنقل سفلي، درج جانبي — لا تصغير لسطح المكتب.
- SEO: sitemap, robots, Open Graph, canonical, Schema.org.

---

## ⏳ لم يُنجز بعد

صريح بلا تجميل. الجداول والمخطط جاهزة لكل ما يلي؛ الناقص هو الواجهات ومنطق
الأعمال.

| المؤجّل | الحالة الفعلية | لماذا |
|---|---|---|
| تخزين S3 | القرص المحلي يعمل بالكامل مع روابط موقّعة | التبديل تنفيذ واحد لـ`StorageProvider`؛ التوقيع والتحقق لا يتغيّران |
| تقرير الزيارة PDF | التصدير إلى CSV يعمل | يتطلب خدمة تصيير |
| التوظيف (المرشحون) | المخطط كامل والجداول جاهزة | لا واجهة بعد |
| رحلة طلب الخدمة | الكتالوج يعمل ويُعرض عامًّا | تحتاج عرض سعر ودفع — الرفع صار متاحًا |
| الفوترة الفعلية | `BillingProvider` مجرّد + حساب الضريبة مختبَر | يحتاج مفاتيح مزوّد (Moyasar مرشّح) |
| إرسال الإشعارات خارجيًا | القناة داخل النظام **تعمل فعليًا**؛ الخارجية لها واجهة | لا مزوّد بريد/SMS مضبوط — و`sentAt` تبقى فارغة فلا نزعم إرسالًا |
| i18n كامل | البنية RTL/LTR جاهزة | النصوص مضمّنة عربيًا؛ لم تُستخرج لملفات ترجمة |
| Playwright E2E رسمي | التحقق تم بسكربت متصفح فعلي | لم تُكتب مجموعة `*.spec.ts` |

---

## ⚠️ مخاطر قائمة

1. **`checkRateLimit` داخل الذاكرة.** مع أكثر من نسخة يصبح الحد لكل نسخة لا
   عالميًا. استبدل المخزن بـRedis قبل نشر متعدد النسخ — الواجهة لن تتغيّر.
2. **`/privacy` و`/terms` مسوّدتان.** موسومتان بتنويه داخل الصفحة. تحتاجان
   مراجعة قانونية قبل الإطلاق التجاري.
3. **٥ ثغرات في اعتماديات التطوير فقط** (`prisma` CLI عبر `deepmerge-ts`،
   و`vite`/`esbuild` عبر Vitest). لا تصل إلى حزمة الإنتاج. تُراجَع عند صدور
   إصلاحات المنبع.
4. **GitHub Pages لا يستضيف المنصة.** موثّق في `DEPLOYMENT.md`. موقع المنيو
   القديم لا يزال يعمل من `legacy/menu-demo/`.
5. **رفع الملفات غير موصول بمخزن.** المخطط يحفظ `storageKey` ومخطط التحقق
   يفرض النوع والحجم، لكن لا مخزن فعلي بعد (S3 أو ما يعادله).
6. **لا نسخ احتياطي مُعرَّف.** يجب ضبطه على قاعدة الإنتاج قبل الإطلاق.

---

## أخطاء حقيقية أمسكتها الفحوصات أثناء البناء

توثيق لأن هذا يثبت أن الفحوصات ليست شكلية:

| الخطأ | من أمسكه | الإصلاح |
|---|---|---|
| `emailSchema` يرفض بريدًا بمسافات زائدة | اختبار وحدة | التنظيف قبل التحقق عبر `.pipe()` |
| مستخدم بلا بيانات اعتماد بعد فشل زرع جزئي | فشل دخول فعلي | `ensureUser` يتحقق من حساب credential لا من المستخدم |
| ٤ صفحات تلتف على طبقة الخدمات | قاعدة ESLint المعمارية | إنشاء `services/catalog.ts` |
| بناء إنتاج بمفتاح تطويري | حارس الإقلاع | توليد مفتاح حقيقي |
| محور الرسم بالتقويم الهجري فتتكرر الشهور | مراجعة بصرية للقطة | تثبيت `ar-SA-u-ca-gregory` للبيانات |
| `PERIOD_DAYS` قيمة تُستخدم كنوع فقط | ESLint | تحويلها إلى `type` صريح |
| `setState` متزامن داخل `useEffect` في العدّاد ومبدّل الوضع | قاعدة `react-hooks` | مُهيّئ حالة كسول + `useSyncExternalStore` |
| `toResult` غير عام فيكسر أنواع الحمولات | TypeScript strict | جعلها عامة في `T` |
| تحميل الإشعارات داخل `useEffect` | قاعدة `react-hooks` | نقل التحميل إلى معالج النقر — الفتح حدث لا حالة تُزامَن |
| **كلمة المرور تظهر في شريط العنوان** | تشغيل Playwright حقيقي | `method="post"` على نموذجي الدخول والتسجيل + اختبار يحرس ذلك |
| رفض الصلاحية يعطي صفحة خطأ ٥٠٠ | تشغيل Playwright حقيقي | حدّ أخطاء + صفحة ٤٠٤ + مكوّن «لا تملك صلاحية» |
| تعليم الدعوة «منتهية» يتراجع مع المعاملة | اختبار تكامل | نقل الكتابة خارج المعاملة قبل الرمي |
| قيد الفرع يمحو قيد المعرّف في نفس `where` | مراجعة الفرق | فصل الشرطين بـ`AND` |

الأخيران يستحقان وقفة: مبدّل الوضع كان يزامن حالة React مع الـDOM داخل تأثير،
وهو نمط يسبب تصييرًا متتاليًا ووميضًا. الإصلاح لم يكن إسكات القاعدة بل تغيير
المصدر: سكربت يسبق أول رسم + `useSyncExternalStore` يقرأ الـDOM مباشرة.


### وقفة على تسريب كلمة المرور

أخطر ما ظهر في هذه الجولة لم يظهر في أي فحص ساكن. عند قيادة متصفح حقيقي
انتهى المطاف بالعنوان إلى:

```
/login?email=viewer%40demo.itqan.sa&password=Itqan%23Demo2026
```

السبب أن النموذج بلا `method`، والافتراضي في HTML هو GET. الإرسال يُعالَج في
JavaScript، لكن إن ضغط المستخدم قبل اكتمال ترطيب React — وهو وارد على شبكة
بطيئة — يُرسل المتصفح النموذج بنفسه. فتذهب كلمة المرور إلى شريط العنوان، ومنه
إلى سجل المتصفح وسجلات الخادم وترويسة الإحالة.

الإصلاح `method="post"` على النموذجين، والتحقق تم بحجب سكربتات Next كليًا ثم
الإرسال يدويًا: لم يعد الرابط يحمل شيئًا. ويحرسه اختبار يمسح كل ملف `.tsx`
فيه حقل كلمة مرور ويشترط أن يفتح كل `<form>` فيه بـ`method="post"` — جُرّب
بإزالة السمة فسقط الاختبار فعلًا.

---

## الخطوة التالية المقترحة

بالترتيب حسب العائد التشغيلي:

1. ~~**ربط Moyasar**~~ — تم. `BillingProvider` صار له تنفيذ حقيقي، والتحصيل
   يُفعَّل بضبط `BILLING_PROVIDER=moyasar` ومفاتيحه في البيئة. بلا مفاتيح
   تقول الواجهة صراحة إن البوابة غير مفعّلة ولا تدّعي تحصيلًا.
2. ~~**مزوّد بريد**~~ — تم. طبقة `EmailProvider` بتنفيذ Resend، ونظام دعوات
   كامل: رمز مبصوم، منع ترقية الصلاحيات، وحد للباقة يشمل الدعوات المعلّقة.
   بلا مفاتيح تُنشأ الدعوة ويُعطى الداعي رابطًا ينقله بنفسه بدل ادّعاء إرسال.
3. **رحلة طلب الخدمة** — الرفع والكتالوج جاهزان؛ الناقص عرض السعر والدفع.
4. **استخراج النصوص** إلى ملفات ترجمة لتفعيل الإنجليزية بالكامل.
5. **تقارير PDF** عبر خدمة تصيير.

### ملاحظة على التخزين

التنفيذ الحالي على القرص المحلي، ويعمل بالكامل لنشر بنسخة واحدة بقرص دائم.
للنشر متعدد النسخ نفّذ `StorageProvider` على S3 — التوقيع والتحقق وفحص
الملكية مستقلة عن مكان التخزين فلن تتغيّر.
